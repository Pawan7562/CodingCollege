import React from 'react';
import { Link as RouterLink } from 'react-router-dom';
import {
  Container,
  Typography,
  Button,
  Box,
  Grid,
  Card,
  CardContent,
  Avatar,
  Chip,
  Paper,
  Fade,
  Slide,
  Grow,
  useTheme,
  alpha,
} from '@mui/material';
import {
  School,
  TrendingUp,
  AccessTime,
  Star,
  PlayCircleFilled,
  People,
  Computer,
  Language,
  Business,
  Psychology,
  Code,
  DesignServices,
  Analytics,
  CloudQueue,
  Security,
  AutoAwesome,
  RocketLaunch,
  Lightbulb,
  EmojiEvents,
  WorkspacePremium,
} from '@mui/icons-material';
// import { motion } from 'framer-motion';
// import AnimatedBackground from '../components/AnimatedBackground';

const categories = [
  { name: 'Web Development', icon: <Code />, color: '#3b82f6' },
  { name: 'Data Science', icon: <Analytics />, color: '#10b981' },
  { name: 'Cloud Computing', icon: <CloudQueue />, color: '#f59e0b' },
  { name: 'Cybersecurity', icon: <Security />, color: '#ef4444' },
  { name: 'UI/UX Design', icon: <DesignServices />, color: '#8b5cf6' },
  { name: 'Business', icon: <Business />, color: '#ec4899' },
];

const features = [
  {
    icon: <RocketLaunch />,
    title: 'Launch Your Career',
    description: 'Get job-ready skills with industry-relevant courses taught by experts.',
  },
  {
    icon: <Lightbulb />,
    title: 'Learn at Your Pace',
    description: 'Flexible learning schedules with lifetime access to course materials.',
  },
  {
    icon: <WorkspacePremium />,
    title: 'Certified Programs',
    description: 'Earn recognized certificates that validate your expertise.',
  },
  {
    icon: <People />,
    title: 'Expert Instructors',
    description: 'Learn from industry professionals with real-world experience.',
  },
];

const stats = [
  { number: '50K+', label: 'Students Enrolled', icon: <School /> },
  { number: '1000+', label: 'Expert Instructors', icon: <People /> },
  { number: '500+', label: 'Premium Courses', icon: <Computer /> },
  { number: '98%', label: 'Success Rate', icon: <TrendingUp /> },
];

const HomeProfessional: React.FC = () => {
  const theme = useTheme();

  return (
    <>
      {/* <AnimatedBackground /> */}
      
      {/* Hero Section */}
      <Box
        sx={{
          minHeight: '100vh',
          display: 'flex',
          alignItems: 'center',
          position: 'relative',
          zIndex: 1,
          background: 'transparent',
        }}
      >
        <Container maxWidth="lg">
          <Grid container spacing={6} alignItems="center">
            <Grid item xs={12} md={6}>
              <div
                initial={{ opacity: 0, x: -50 }}
                animate={{ opacity: 1, x: 0 }}
                transition={{ duration: 0.8 }}
              >
                <Typography
                  variant="h2"
                  component="h1"
                  sx={{
                    fontWeight: 800,
                    color: 'white',
                    mb: 3,
                    textShadow: '2px 2px 4px rgba(0,0,0,0.3)',
                    fontSize: { xs: '2.5rem', md: '3.5rem' },
                  }}
                >
                  Transform Your Future with
                  <Box component="span" sx={{ color: '#fbbf24', ml: 2 }}>
                    World-Class Education
                  </Box>
                </Typography>
                
                <Typography
                  variant="h6"
                  sx={{
                    color: 'rgba(255,255,255,0.9)',
                    mb: 4,
                    fontSize: { xs: '1rem', md: '1.2rem' },
                    lineHeight: 1.6,
                  }}
                >
                  Unlock your potential with cutting-edge courses designed by industry experts. 
                  Join thousands of learners advancing their careers today.
                </Typography>

                <Box sx={{ display: 'flex', gap: 2, flexWrap: 'wrap' }}>
                  <motion.div whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}>
                    <Button
                      component={RouterLink}
                      to="/courses"
                      variant="contained"
                      size="large"
                      sx={{
                        px: 4,
                        py: 1.5,
                        fontSize: '1.1rem',
                        fontWeight: 600,
                        background: 'linear-gradient(45deg, #3b82f6, #8b5cf6)',
                        boxShadow: '0 4px 15px rgba(59, 130, 246, 0.4)',
                        '&:hover': {
                          background: 'linear-gradient(45deg, #2563eb, #7c3aed)',
                          boxShadow: '0 6px 20px rgba(59, 130, 246, 0.6)',
                        },
                      }}
                      startIcon={<PlayCircleFilled />}
                    >
                      Explore Courses
                    </Button>
                  </motion.div>
                  
                  <motion.div whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}>
                    <Button
                      component={RouterLink}
                      to="/register"
                      variant="outlined"
                      size="large"
                      sx={{
                        px: 4,
                        py: 1.5,
                        fontSize: '1.1rem',
                        fontWeight: 600,
                        borderColor: 'white',
                        color: 'white',
                        '&:hover': {
                          borderColor: 'white',
                          backgroundColor: 'rgba(255,255,255,0.1)',
                        },
                      }}
                    >
                      Get Started Free
                    </Button>
                  </motion.div>
                </Box>
              </motion.div>
            </Grid>
            
            <Grid item xs={12} md={6}>
              <motion.div
                initial={{ opacity: 0, scale: 0.8 }}
                animate={{ opacity: 1, scale: 1 }}
                transition={{ duration: 0.8, delay: 0.2 }}
              >
                <Paper
                  elevation={24}
                  sx={{
                    p: 4,
                    background: 'rgba(255,255,255,0.1)',
                    backdropFilter: 'blur(10px)',
                    border: '1px solid rgba(255,255,255,0.2)',
                    borderRadius: 4,
                  }}
                >
                  <Typography variant="h4" sx={{ color: 'white', mb: 3, fontWeight: 700 }}>
                        Why Choose College Platform?
                      </Typography>
                      <Grid container spacing={3}>
                        {features.map((feature, index) => (
                          <Grid item xs={12} sm={6} key={index}>
                            <motion.div
                              whileHover={{ scale: 1.02 }}
                              transition={{ type: 'spring', stiffness: 300 }}
                            >
                              <Box sx={{ display: 'flex', alignItems: 'flex-start', gap: 2 }}>
                                <Avatar
                                  sx={{
                                    bgcolor: alpha('#ffffff', 0.2),
                                    color: 'white',
                                    mb: 1,
                                  }}
                                >
                                  {feature.icon}
                                </Avatar>
                                <Box>
                                  <Typography
                                    variant="h6"
                                    sx={{ color: 'white', fontWeight: 600, mb: 1 }}
                                  >
                                    {feature.title}
                                  </Typography>
                                  <Typography
                                    variant="body2"
                                    sx={{ color: 'rgba(255,255,255,0.8)' }}
                                  >
                                    {feature.description}
                                  </Typography>
                                </Box>
                              </Box>
                            </motion.div>
                          </Grid>
                        ))}
                      </Grid>
                    </Paper>
                  </motion.div>
            </Grid>
          </Grid>
        </Container>
      </Box>

      {/* Stats Section */}
      <Box sx={{ py: 8, background: 'rgba(255,255,255,0.95)', position: 'relative', zIndex: 2 }}>
        <Container maxWidth="lg">
          <motion.div
            initial={{ opacity: 0, y: 50 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
          >
            <Typography
              variant="h3"
              align="center"
              sx={{ mb: 6, fontWeight: 700, color: '#1f2937' }}
            >
              Trusted by <Box component="span" sx={{ color: '#3b82f6' }}>Learners Worldwide</Box>
            </Typography>
          </motion.div>
          
          <Grid container spacing={4} justifyContent="center">
            {stats.map((stat, index) => (
              <Grid item xs={6} md={3} key={index}>
                <motion.div
                  initial={{ opacity: 0, scale: 0.5 }}
                  whileInView={{ opacity: 1, scale: 1 }}
                  transition={{ duration: 0.5, delay: index * 0.1 }}
                  viewport={{ once: true }}
                  whileHover={{ scale: 1.05 }}
                >
                  <Card
                    sx={{
                      textAlign: 'center',
                      py: 3,
                      background: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)',
                      color: 'white',
                      boxShadow: '0 10px 30px rgba(0,0,0,0.1)',
                      transform: 'translateY(0)',
                      transition: 'all 0.3s ease',
                      '&:hover': {
                        transform: 'translateY(-10px)',
                        boxShadow: '0 20px 40px rgba(0,0,0,0.2)',
                      },
                    }}
                  >
                    <Avatar
                      sx={{
                        bgcolor: 'rgba(255,255,255,0.2)',
                        mx: 'auto',
                        mb: 2,
                        width: 60,
                        height: 60,
                      }}
                    >
                      {stat.icon}
                    </Avatar>
                    <Typography variant="h3" sx={{ fontWeight: 800, mb: 1 }}>
                      {stat.number}
                    </Typography>
                    <Typography variant="body1" sx={{ opacity: 0.9 }}>
                      {stat.label}
                    </Typography>
                  </Card>
                </motion.div>
              </Grid>
            ))}
          </Grid>
        </Container>
      </Box>

      {/* Categories Section */}
      <Box sx={{ py: 8, background: 'white', position: 'relative', zIndex: 2 }}>
        <Container maxWidth="lg">
          <motion.div
            initial={{ opacity: 0, y: 50 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
          >
            <Typography
              variant="h3"
              align="center"
              sx={{ mb: 6, fontWeight: 700, color: '#1f2937' }}
            >
              Explore <Box component="span" sx={{ color: '#3b82f6' }}>Popular Categories</Box>
            </Typography>
          </motion.div>
          
          <Grid container spacing={4}>
            {categories.map((category, index) => (
              <Grid item xs={6} sm={4} md={2} key={index}>
                <motion.div
                  initial={{ opacity: 0, scale: 0.8 }}
                  whileInView={{ opacity: 1, scale: 1 }}
                  transition={{ duration: 0.5, delay: index * 0.1 }}
                  viewport={{ once: true }}
                  whileHover={{ scale: 1.05, rotate: 5 }}
                >
                  <Card
                    sx={{
                      textAlign: 'center',
                      py: 3,
                      cursor: 'pointer',
                      transition: 'all 0.3s ease',
                      '&:hover': {
                        transform: 'translateY(-10px)',
                        boxShadow: '0 20px 40px rgba(0,0,0,0.1)',
                      },
                    }}
                  >
                    <Avatar
                      sx={{
                        bgcolor: category.color,
                        mx: 'auto',
                        mb: 2,
                        width: 60,
                        height: 60,
                      }}
                    >
                      {category.icon}
                    </Avatar>
                    <Typography variant="body1" sx={{ fontWeight: 600 }}>
                      {category.name}
                    </Typography>
                  </Card>
                </motion.div>
              </Grid>
            ))}
          </Grid>
        </Container>
      </Box>

      {/* CTA Section */}
      <Box
        sx={{
          py: 12,
          background: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)',
          position: 'relative',
          zIndex: 2,
        }}
      >
        <Container maxWidth="md">
          <motion.div
            initial={{ opacity: 0, y: 50 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
          >
            <Typography
              variant="h3"
              align="center"
              sx={{ mb: 3, fontWeight: 700, color: 'white' }}
            >
              Ready to Start Your Learning Journey?
            </Typography>
            <Typography
              variant="h6"
              align="center"
              sx={{ mb: 4, color: 'rgba(255,255,255,0.9)' }}
            >
              Join thousands of students who are already transforming their careers
            </Typography>
            <Box sx={{ display: 'flex', justifyContent: 'center', gap: 2 }}>
              <motion.div whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }}>
                <Button
                  component={RouterLink}
                  to="/register"
                  variant="contained"
                  size="large"
                  sx={{
                    px: 6,
                    py: 2,
                    fontSize: '1.1rem',
                    fontWeight: 600,
                    background: 'white',
                    color: '#3b82f6',
                    '&:hover': {
                      background: 'rgba(255,255,255,0.9)',
                    },
                  }}
                  startIcon={<RocketLaunch />}
                >
                  Get Started Now
                </Button>
              </motion.div>
            </Box>
          </motion.div>
        </Container>
      </Box>
    </>
  );
};

export default HomeProfessional;
